\ \\ \noindent {Useful functions and procedures}
  \label{alg:utils}
  \begin{algorithmic}[1]
    \Function{genKeys}{}
    \EndFunction
    \Function{receipt}{\texttt{channel}}
      \State parse \texttt{channel} as $\left(\alice, \bob, c_{\alice},
      c_{\bob}, \mathtt{htlcs}, \mathtt{verNo}, \mathit{pchid}\right)$
      \TODO{check if channel needs verNo}
      \State \Return $\left(\left(\alice, c_{\alice}\right),
      \left(\bob, c_{\bob}\right), \mathit{pchid}\right)$
    \EndFunction
    \Function{fundingTX}{funder, fundee}
      \State \Return TX \{\}
    \EndFunction
    \Function{isFundingTX}{tx, funder, fundee}
      \If{tx = \texttt{fundingTX}(funder, fundee)}
        \State \Return True
      \Else
        \State \Return False
      \EndIf
    \EndFunction
    \Function{commitmentTX}{fundingTX, $c_1$, $c_2$, htlcs}
      \State \Return TX \{\} \TODO{add keys, delays}
    \EndFunction
    \Function{iscommitmentTX}{tx, $c_1$, $c_2$, htlcs}
      \If{tx = \texttt{commitmentTX}($c_1$, $c_2$, htlcs)}
        \State \Return True
      \Else
        \State \Return False
      \EndIf
    \EndFunction
    \Function{htlcTX}{commitmentTX, $c$}
      \State \Return TX \{\} \TODO{add keys, delays}
    \EndFunction
    \Function{isFundingTX}{tx, funder, fundee}
      \If{tx = \texttt{fundingTX}(funder, fundee)}
        \State \Return True
      \Else
        \State \Return False
      \EndIf
    \EndFunction
  \end{algorithmic}
\hrulefill
